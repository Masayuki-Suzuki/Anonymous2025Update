query Archives {
    archives{
        documentId
        title
        slug
        posts(filters: { publishedAt: { ne: null } }, hasTags: false){
            documentId
            title
            slug
            publishedAt
        }
    }
}
