query Posts {
    posts {
        documentId
        title
        excerpt
        thumbnail {
            url
            alternativeText
        }
        tags {
            name
            slug
        }
        createdAt
        updatedAt
    }
}
